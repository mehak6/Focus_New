<Window x:Class="FocusVoucherSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FocusVoucherSystem"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="800" 
        Width="1200"
        WindowState="Maximized"
        KeyDown="Window_KeyDown">
    
    <Window.Resources>
        <!-- Global Styles -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Background" Value="#f0f0f0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#ccc"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e0e0e0"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter Property="Background" Value="#d0d0d0"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="StatusBarStyle" TargetType="StatusBar">
            <Setter Property="Background" Value="#2d3748"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Top Menu Bar -->
        <Menu DockPanel.Dock="Top" Background="#4a5568" Foreground="White" Height="25">
            <MenuItem Header="_File">
                <MenuItem Header="_New Company" Command="{Binding NewCompanyCommand}"/>
                <MenuItem Header="_Switch Company" ItemsSource="{Binding Companies}">
                    <MenuItem.ItemContainerStyle>
                        <Style TargetType="MenuItem">
                            <Setter Property="Header" Value="{Binding Name}"/>
                            <Setter Property="Command" Value="{Binding DataContext.ChangeCompanyCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                            <Setter Property="CommandParameter" Value="{Binding}"/>
                        </Style>
                    </MenuItem.ItemContainerStyle>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Import Legacy Data" Command="{Binding ImportDataCommand}"/>
                <MenuItem Header="_Export Data" Command="{Binding ExportDataCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Refresh" Command="{Binding RefreshDataCommand}" InputGestureText="F5"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_Hotkey Reference" Command="{Binding ShowHotkeysCommand}" InputGestureText="F1"/>
                <MenuItem Header="_About" Command="{Binding AboutCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Bottom Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" Style="{StaticResource StatusBarStyle}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Company: " Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding CurrentCompany.Name}" FontWeight="Bold" Margin="0,0,15,0"/>
                    <TextBlock Text="User: " Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding CurrentUser}" FontWeight="Bold"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <!-- Main Content Area -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Tab Navigation Bar -->
            <Border Grid.Row="0" Background="#f8f9fa" BorderBrush="#dee2e6" BorderThickness="0,0,0,1" Padding="10,8">
                <StackPanel Orientation="Horizontal">
                    <Button Content="?? Voucher Entry" 
                            Style="{StaticResource TabButtonStyle}"
                            Command="{Binding NavigateToVoucherEntryCommand}"
                            ToolTip="F2 - Add vouchers and manage entries"/>
                    <Button Content="?? Vehicle Management" 
                            Style="{StaticResource TabButtonStyle}"
                            Command="{Binding NavigateToVehicleManagementCommand}"
                            ToolTip="F1 - Manage vehicles and accounts"/>
                    <Button Content="?? Reports" 
                            Style="{StaticResource TabButtonStyle}"
                            Command="{Binding NavigateToReportsCommand}"
                            ToolTip="F3 - View and print reports"/>
                    <Button Content="?? Utilities" 
                            Style="{StaticResource TabButtonStyle}"
                            Command="{Binding NavigateToUtilitiesCommand}"
                            ToolTip="F4 - System utilities and settings"/>
                </StackPanel>
            </Border>

            <!-- Content Host -->
            <Border Grid.Row="1" Background="White" Margin="0">
                <ContentControl x:Name="ContentHost"/>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Grid Visibility="{Binding IsBusy, Converter={x:Static local:Converters.BooleanToVisibilityConverter}}"
              Background="#80000000">
            <Border Background="White" 
                    CornerRadius="10" 
                    Padding="30"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    MinWidth="250">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar IsIndeterminate="True" Width="20" Height="20" Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding BusyMessage}" 
                               FontSize="14" 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Error Message -->
        <Grid Visibility="{Binding HasError, Converter={x:Static local:Converters.BooleanToVisibilityConverter}}"
              Background="#80000000">
            <Border Background="#dc3545" 
                    CornerRadius="10" 
                    Padding="20"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    MaxWidth="500">
                <StackPanel>
                    <TextBlock Text="Error" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding ErrorMessage}" 
                               FontSize="12" 
                               Foreground="White" 
                               TextWrapping="Wrap"
                               Margin="0,0,0,15"/>
                    <Button Content="OK" 
                            Command="{Binding ClearErrorsCommand}"
                            Background="White"
                            Foreground="#dc3545"
                            Padding="20,5"
                            HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>


