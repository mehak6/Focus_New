<UserControl x:Class="FocusVoucherSystem.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2d3748"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="#cbd5e0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#4299e1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3182ce"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#e2e8f0"/>
            <Setter Property="Foreground" Value="#2d3748"/>
            <Setter Property="BorderBrush" Value="#cbd5e0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#f7fafc" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1" Padding="20,15">
            <StackPanel>
                <TextBlock Text="📊 Reports" Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Text="Generate and export reports" FontSize="12" Foreground="#718096"/>
            </StackPanel>
        </Border>

        <!-- Filters and Actions -->
        <Border Grid.Row="1" Background="White" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Filter Row -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="280"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Report Type:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding ReportTypes}" SelectedItem="{Binding SelectedReportType}" Height="32"/>

                    <TextBlock Grid.Column="3" Text="From Date:" Style="{StaticResource LabelStyle}"/>
                    <DatePicker Grid.Column="4" SelectedDate="{Binding StartDate}" Height="32"/>

                    <TextBlock Grid.Column="6" Text="To Date:" Style="{StaticResource LabelStyle}"/>
                    <DatePicker Grid.Column="7" SelectedDate="{Binding EndDate}" Height="32"/>

                    <TextBlock Grid.Column="9" Text="Vehicle:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox Grid.Column="10" ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}"
                              DisplayMemberPath="DisplayName" Height="32"/>
                </Grid>
                
                <!-- Action Buttons Row -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- Generate Button -->
                        <Button Content="📊 Generate Report" Command="{Binding GenerateReportCommand}" 
                                Style="{StaticResource PrimaryButtonStyle}" Width="140" Height="38" Margin="5,0"/>
                        
                        <!-- Export Buttons -->
                        <Button Content="📄 Export CSV" Command="{Binding ExportCsvCommand}" 
                                Style="{StaticResource SecondaryButtonStyle}" Width="110" Height="38" Margin="5,0"/>
                        <Button Content="📋 Export Excel" Command="{Binding ExportExcelCommand}" 
                                Style="{StaticResource SecondaryButtonStyle}" Width="120" Height="38" Margin="5,0"/>
                        <Button Content="📑 Export PDF" Command="{Binding ExportPdfCommand}" 
                                Style="{StaticResource SecondaryButtonStyle}" Width="110" Height="38" Margin="5,0"/>
                        
                        <!-- Print Button -->
                        <Button Content="🖨️ Print Report" Command="{Binding PrintReportCommand}" 
                                Style="{StaticResource SecondaryButtonStyle}" Width="120" Height="38" Margin="5,0"/>
                        
                        <!-- Refresh Button -->
                        <Button Content="🔄 Refresh" Command="{Binding RefreshDataCommand}" 
                                Style="{StaticResource SecondaryButtonStyle}" Width="90" Height="38" Margin="5,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Results Grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding ReportRows}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternationCount="2"
                  AlternatingRowBackground="#f8f9fa"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="V.No" Binding="{Binding VoucherNumber}" Width="80"/>
                <DataGridTextColumn Header="Vehicle" Binding="{Binding VehicleNumber}" Width="180"/>
                <DataGridTextColumn Header="Narration" Binding="{Binding Narration}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N2}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Right"/>
                            <Setter Property="FontFamily" Value="Consolas"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Dr/Cr" Binding="{Binding DrCr}" Width="60"/>
                <DataGridTextColumn Header="Balance" Binding="{Binding RunningBalance, StringFormat=N2}" Width="140">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Right"/>
                            <Setter Property="FontFamily" Value="Consolas"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals and Status -->
        <Border Grid.Row="3" Background="#edf2f7" BorderBrush="#cbd5e0" BorderThickness="0,1,0,0" Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" FontSize="12"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Margin="0,0,20,0">
                        <Run Text="Total Dr: "/>
                        <Run Text="{Binding TotalDebits, StringFormat=N2}" FontWeight="Bold"/>
                    </TextBlock>
                    <TextBlock Margin="0,0,20,0">
                        <Run Text="Total Cr: "/>
                        <Run Text="{Binding TotalCredits, StringFormat=N2}" FontWeight="Bold"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Net: "/>
                        <Run Text="{Binding NetAmount, StringFormat=N2}" FontWeight="Bold"/>
                        <Run Text=" "/>
                        <Run Text="{Binding NetDrCr}" FontWeight="Bold"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
