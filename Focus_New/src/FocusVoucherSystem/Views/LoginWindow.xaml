<Window x:Class="FocusVoucherSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Focus Voucher System - Login"
        Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f7fafc"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4299e1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3182ce"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="10" BorderBrush="#cbd5e0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#4299e1" CornerRadius="10,10,0,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="Focus Voucher System" FontSize="24" FontWeight="Bold"
                               Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="Enter password to access data" FontSize="12"
                               Foreground="White" Opacity="0.9" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Login Form -->
            <Grid Grid.Row="1" Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Lock Icon -->
                <Viewbox Grid.Row="0" Width="80" Height="80" Margin="0,20,0,0">
                    <Canvas Width="24" Height="24">
                        <Path Fill="#cbd5e0" Data="M12,1 C9.24,1 7,3.24 7,6 L7,8 C5.9,8 5,8.9 5,10 L5,20 C5,21.1 5.9,22 7,22 L17,22 C18.1,22 19,21.1 19,20 L19,10 C19,8.9 18.1,8 17,8 L17,6 C17,3.24 14.76,1 12,1 M12,3 C13.68,3 15,4.32 15,6 L15,8 L9,8 L9,6 C9,4.32 10.32,3 12,3 M12,13 C13.1,13 14,13.9 14,15 C14,16.1 13.1,17 12,17 C10.9,17 10,16.1 10,15 C10,13.9 10.9,13 12,13 Z"/>
                    </Canvas>
                </Viewbox>

                <!-- Password Label -->
                <TextBlock Grid.Row="1" Text="Password" FontWeight="SemiBold" FontSize="14"
                           Foreground="#2d3748" Margin="0,20,0,8"/>

                <!-- Password Input -->
                <PasswordBox x:Name="PasswordInput" Grid.Row="2" Height="45" FontSize="16"
                             Padding="15,10" BorderBrush="#cbd5e0" BorderThickness="1"
                             KeyDown="PasswordInput_KeyDown">
                    <PasswordBox.Style>
                        <Style TargetType="PasswordBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="PasswordBox">
                                        <Border Background="White" BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </PasswordBox.Style>
                </PasswordBox>

                <!-- Error Message -->
                <TextBlock x:Name="ErrorMessage" Grid.Row="3" Foreground="#e53e3e" FontSize="12"
                           Margin="0,8,0,0" Visibility="Collapsed" TextWrapping="Wrap"/>

                <!-- Login Button -->
                <Button Grid.Row="4" Content="LOGIN" Style="{StaticResource ModernButton}"
                        Height="45" Margin="0,20,0,0" Click="LoginButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
